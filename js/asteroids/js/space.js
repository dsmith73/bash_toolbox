
function handleSpace() {
    ctx.fillStyle = '#000000'
    ctx.fillRect(0, 0, canvas.width, canvas.height)
}

let stars = []

// draw stars  
// for (let i=0; i < STARS_NUM; i++) {
//     ctx.fillStyle       = '#e6b000'
//     ctx.beginPath()
//     ctx.arc(stars[i].x, stars[i].y, SHIP_SIZE /15, 0, Math.PI *2, false)
//     ctx.fill()
// }

